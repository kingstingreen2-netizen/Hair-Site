# Hair-Site

Simple static website for a hair studio.

## Run locally

Open `index.html` directly in your browser, or serve with:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.
